export enum AppRoute {
  MyQuests = '/myQuests',
  Root = '/',
  Login = '/login',
  Quest = '/quest',
  Load = '/load',
  Booking = '/booking',
  Contacts = '/contacts',
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}
